{
  "name": "exercicio",
  "version": "1.0.0",
  "scripts": {
    "build": "sass ./src/scss:./dist/css --no-source-map",
    "watch": "sass --watch ./src/scss:./dist/css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.79.4"
  }
}
